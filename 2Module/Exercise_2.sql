# Find the distinct values for the extended step. 
# The code has been started for you, but you will need to program the third line yourself before running the query.

SELECT
DISTINCT Extend_step
FROM salary_range_by_job_classification


